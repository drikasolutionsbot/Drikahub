import { LegoLoader } from "@/components/ui/lego-loader";

export default function GiveawaysPage() {
  return (
    <LegoLoader
      title="Aguardando liberação da proprietária"
      description="O sistema de sorteios está aguardando a liberação da proprietária para ser ativado."
    />
  );
}
